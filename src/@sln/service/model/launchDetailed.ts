/**
 * Generated by orval v6.8.1 🍺
 * Do not edit manually.
 * Launch Library
 * The Launch Library API is a product by The Space Devs with an up-to-date database of Spaceflight events. 

While this API is free to use it is subject to rate limiting for non-authenticated requests.

Please use https://lldev.thespacedevs.com for development testing - the development endpoint has stale data but is not subject to any rate limits.

If you are interested in a higher rate limit please consider supporting the project on Patreon for access to an API Key.
 * OpenAPI spec version: v2.2.0
 */
import type { LaunchStatus } from "./launchStatus";
import type { Update } from "./update";
import type { AgencySerializerDetailedCommon } from "./agencySerializerDetailedCommon";
import type { RocketDetailed } from "./rocketDetailed";
import type { Mission } from "./mission";
import type { Pad } from "./pad";
import type { InfoURL } from "./infoURL";
import type { VidURL } from "./vidURL";
import type { Program } from "./program";
import type { MissionPatch } from "./missionPatch";

export interface LaunchDetailed {
  readonly id: string;
  readonly url: string;
  slug: string;
  readonly flightclub_url: string | null;
  readonly r_spacex_api_id: string | null;
  readonly name: string;
  readonly status: LaunchStatus;
  readonly last_updated: string;
  readonly updates: Update[];
  readonly net: string | null;
  readonly window_end: string | null;
  readonly window_start: string | null;
  readonly probability: number | null;
  readonly holdreason: string | null;
  readonly failreason: string | null;
  readonly hashtag: string | null;
  readonly launch_service_provider: AgencySerializerDetailedCommon;
  readonly rocket: RocketDetailed;
  readonly mission: Mission;
  readonly pad: Pad;
  readonly infoURLs: InfoURL[];
  readonly vidURLs: VidURL[];
  readonly webcast_live: boolean;
  readonly image: string;
  readonly infographic: string;
  readonly program: Program[];
  readonly orbital_launch_attempt_count: number;
  readonly location_launch_attempt_count: number;
  readonly pad_launch_attempt_count: number;
  readonly agency_launch_attempt_count: number;
  readonly orbital_launch_attempt_count_year: number;
  readonly location_launch_attempt_count_year: number;
  readonly pad_launch_attempt_count_year: number;
  readonly agency_launch_attempt_count_year: number;
  readonly mission_patches: MissionPatch[];
}
